{
  "name": "nutrirank",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx db/migrate.ts",
    "db:seed": "tsx db/seed.ts",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "ingest": "tsx scripts/ingest/index.ts",
    "regrade": "tsx scripts/regrade/index.ts",
    "build:ingest": "esbuild scripts/ingest/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/ingest.mjs --external:better-sqlite3 --external:drizzle-orm --external:drizzle-orm/*"
  },
  "dependencies": {
    "better-sqlite3": "latest",
    "drizzle-orm": "latest",
    "next": "^15",
    "react": "^19",
    "react-dom": "^19",
    "recharts": "^3.10.0"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/better-sqlite3": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "autoprefixer": "latest",
    "drizzle-kit": "latest",
    "esbuild": "latest",
    "postcss": "latest",
    "tailwindcss": "^3",
    "tsx": "latest",
    "typescript": "^5",
    "vitest": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
